"use client";

const Background = () => {
  return (
    <div 
      className="fixed top-0 left-0 w-full h-full -z-10 bg-gradient-to-br from-[#0d0d1a] via-[#1a0d1d] to-[#0d0d1a] animate-gradient-xy" 
    />
  );
};

export default Background;
